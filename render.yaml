services:
  - type: web
    name: tiktok-age-checker
    runtime: python
    buildCommand: |
      pip install -r requirements.txt && 
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 playwright install chromium && 
      playwright install-deps
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: REDIS_HOST
        value: https://refined-mule-19659.upstash.io  # ← Fill this!
      - key: REDIS_PORT
        value: "6379"  # ← Fill this!
      - key: REDIS_PASSWORD
        value: AUzLAAIjcDFjNzY3MDY2YmQ1MDc0NWVmOTJmY2RkMGVmZWJmOWI0ZXAxMA  # ← Fill this!
